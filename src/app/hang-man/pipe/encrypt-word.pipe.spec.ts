import { EncryptWordPipe } from './encrypt-word.pipe';

describe('EncryptWordPipe', () => {
  it('create an instance', () => {
    const pipe = new EncryptWordPipe();
    expect(pipe).toBeTruthy();
  });
});
