import { MemoCardsStore } from './memo-cards.store';

describe('MemoCardsStoreStore', () => {
  const componentStore = new MemoCardsStore();

  it('should be created', () => {
    expect(componentStore).toBeTruthy();
  });
});
